{
  "name": "@workspace-template/frontend",
  "scripts": {
    "lint": "oxlint --config .oxlintrc.json --tsconfig tsconfig.json --fix --deny-warnings",
    "format": "oxfmt --config .oxfmtrc.json --write",
    "typecheck": "tsc --noEmit",
    "prepare": "cd .. && husky frontend/.husky"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "oxfmt": "0.28.0",
    "oxlint": "1.43.0",
    "typescript": "5.9.3"
  }
}
